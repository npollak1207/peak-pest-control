# Public marketing/analytics IDs, baked into the client bundle at build time.
# These run in the visitor's browser, so they're not secret. Only loaded in
# production builds, so they don't fire during local dev. Anything set in the
# Vercel dashboard overrides what's here.

# Microsoft Clarity — heat maps + session recordings
NEXT_PUBLIC_CLARITY_PROJECT_ID=xlxn8om0zw
